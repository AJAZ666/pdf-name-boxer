---
name: pdf-name-boxer
description: >
  综测证明整理全流程：从评分细则到最终Word（含可选评测表填写）。
  完整闭环：(1)读评分细则理解加分规则（无细则则按常识+用户确认） (2)读每份证明PDF了解活动内容 (3)如用户提供板块评定Excel则参考其分类，无则按活动类型自行判断 (4)判定每个活动属于哪个板块（智育/德育/美育/劳育）、加多少分、归哪个小项 (5)与用户确认分类结果和排序 (6)强制询问用户姓名后，用locate_name.py自动OCR定位姓名页码（可选学号双匹配防同名） (7)用check_stamp.py检查公章，截取说明+公章和名字所在行并画红框 (8)按板块顺序生成带标题的Word (9)可选：按板块填写评测表。
  当用户上传多份综测证明/获奖证书PDF要求"整理综测""分类加分""框出我的名字""生成Word""填评测表"等时使用。
---

# 综测证明整理工具

完整流程：读细则 → 读证明 → 分类判定 → 确认 → 定位姓名 → 查公章 → 截图 → 生成Word → （可选）填评测表。

## 前置依赖

```bash
python -m pip install pymupdf pillow python-docx rapidocr-onnxruntime numpy openpyxl
```

## 完整流程

### 第一步：读评分细则

**有评分细则PDF时：**
1. Read评分细则，提取各板块加分规则：
   - 德育（小项编号、每项加多少分）
   - 智育（小项编号、每场/每次加多少分）
   - 体育、美育、劳育
2. 记录关键条款：哪些活动类型加多少分、归哪个小项

**没有评分细则时：**
1. 按活动类型自行初判：
   - 青马工程、党支部活动、党团学习讲座 → 德育
   - 学术讲座、行业论坛、竞赛 → 智育
   - 文艺汇演、艺术展览 → 美育
   - 志愿劳动、实践活动 → 劳育
2. 明确告诉用户"没有评分细则，以下分类是我按活动类型判断的，请你核对"
3. 不自行编造具体分数和小项编号，让用户补充

### 第二步：读所有证明PDF + 板块评定Excel

**2a. 读所有证明PDF**
1. 逐份Read每份证明PDF的第1页
2. 提取每个活动的：活动名称、主办单位、活动类型（讲座/党日/青马/文体/志愿等）

**2b. 匹配板块评定Excel（如有）**
1. 用openpyxl读取Excel：
   - A列 = 活动名称
   - D列 = 板块（智育/德育/美育/劳育/体育）
2. 对每份PDF，用其活动名称关键词去A列做模糊匹配
   - 例如PDF叫"金交会综测证明.pdf"，搜"金交会"
   - 匹配到就读D列，得到该活动的板块
3. **同一活动多行匹配**：如果多行板块一致，取任一行；如果板块不一致，问用户
4. **Excel中找不到该活动**：单独拎出来，列入"待确认"清单，问用户怎么处理；用户也无法判断的，回评分细则自行判断

### 第三步：分类判定

**优先级：用户明确说的 > 板块评定Excel > 评分细则 > 活动类型自行判断**

1. 板块由Excel（如有）或细则确定
2. **加分数和小项编号**：回评分细则里找对应条款
   - 细则里找不到对应条款 → 单独拎出来问用户，不自行编造
3. 对每个活动，给出：板块 + 加分数 + 小项编号
4. 输出格式示例：`"金交会暨清华五道口全球金融论坛"，+1（1）`
5. **分类有歧义时**：列出两种可能，问用户选哪个，不要自己拍板

### 第四步：与用户确认

1. 把分类结果列成清单给用户看
2. 用户确认或修正后再继续
3. 确认最终排序：默认先智育、再德育、再美育、再劳育，或按用户要求
4. **这一步必须等用户确认后才继续，不要直接跳到截图**

### 第五步：询问姓名并自动定位页码（强制规则）

**必须主动向用户询问姓名，禁止从文件名/PDF内容自行推断姓名**，即使文件名或文件内容里看似明显（如"张三综合评测表.docx"），也一律先问、以用户口头确认为准。用户未提供姓名 → 停下来等回答，不进入下一步。

拿到姓名（可同时向用户要学号，用于防同名校验）后：

1. 运行 `scripts/locate_name.py` 自动逐页定位：
   ```bash
   python scripts/locate_name.py <PDF目录> --name 姓名 [--id 学号] --out locate_result.json
   ```
   - 自动遍历目录下所有PDF、逐页OCR搜索；找不到自动升级zoom（2.0→2.5→3.0→3.5）重试
   - 输出每份PDF的 name_page 和 box 坐标，未找到的文件列入 not_found
2. 核对输出：名字真实存在的保留；`not_found` 清单逐份问用户是否排除
3. 多页名单PDF无需人工翻页，脚本自动扫

### 第六步：检查公章

生成截图前，对说明页（header_page）跑公章检测：

```bash
python scripts/check_stamp.py <PDF文件> --page <说明页>
```

- 输出"检测到公章/未检测到公章"+ 红色像素占比
- **未检测到公章**：告知用户"原文件可能无章（如学校电子证明模板），请确认"，不要静默交付；若纸质扫描件应有章，提示用户检查扫描质量

### 第七步：写配置JSON

在工作目录创建config.json：

```json
{
  "pdf_dir": "PDF所在目录",
  "output": "输出Word完整路径",
  "target_name": "姓名（用户确认过的）",
  "target_id": "学号（可选，防同名误框）",
  "zoom": 2.0,
  "box_pad": 12,
  "tasks": [
    {
      "file": "文件名.pdf",
      "title": "1. 活动名称，+X（小项）",
      "name_page": 1,
      "header_page": 1,
      "half_height": 350
    },
    {
      "file": "说明和名字紧挨着的.pdf",
      "title": "2. 活动名称，+X（小项）",
      "name_page": 1,
      "single_page": true
    }
  ]
}
```

**字段说明：**
- `title`：Word里的标题，格式为"序号. 活动名称，+分数（小项编号）"
- `name_page`：姓名在第几页（来自locate_name.py输出）
- `header_page`：活动说明在第几页（通常和name_page相同，先跑check_stamp.py确认此页有公章）
- `half_height`：以名字为中心上下各截多少像素（zoom=2.0时，300-400合适）
- `single_page: true`：说明和名字紧挨着，只截一张全宽图
- `target_id`：建议配置学号；配置后若页面有同名者，只有同一行带该学号的才命中

### 第八步：运行脚本

```bash
python scripts/box_names.py config.json
```

### 第九步：验证交付

1. 跑通后打开输出Word，确认：
   - 分类标题正确（板块、分数、小项）
   - 每个红框精确框住姓名（有学号配置时红框旁应能看到学号）
   - split模式：说明 + 公章 + **"姓名/学院/学号"表头行** + 名单红框行完整
   - 按板块顺序排列
2. 把Word交付给用户，**交付前确认用户接受本地文件方式**，不擅自上传云端

### 第十步（可选）：填写评测表

用户提供评测表（.docx）并要求填写时：

1. 用python-docx读取评测表，定位各板块表格（德育/智育/美育/劳育/体育）
2. 按"活动名称 + 分数（小项编号）"逐项写入对应板块加分行，保留原单元格格式
3. **汇总行（各板块总分、综合测评总分、名次）默认留空**给测评小组填，除非用户明确要求填写
4. **写副本验证再写回**：先在副本上修改并回读验证内容/格式正确，再写回原文件；原文件先备份
5. 评测表里出现的活动，必须与证明Word里的活动一一对应；证明缺失的活动（如用户自备证书的）单独向用户说明

## 隐私保护（强制）

1. **姓名/学号只写入本地config.json和输出文件**，运行结束临时截图（_tmp_*.png）自动删除（脚本已内置try/finally兜底清理）
2. **日志脱敏**：控制台输出学号只显示前4位+`***`
3. **交付方式由用户决定**：含个人资料的产物（Word/截图）默认只交付本地文件；用户要求上传云端时才上传
4. 中途异常退出也要检查并清理残留的临时截图

## 异常处理

| 情况 | 处理方式 |
|---|---|
| 没有评分细则 | 按活动类型初判，明确告知用户自行核对，不编造分数 |
| 没有板块评定Excel | 按活动名称自行判断，标注"待确认" |
| Excel中找不到该活动 | 单独列入待确认清单问用户；用户判断不了再按细则自行判断 |
| 细则里找不到对应分数/小项 | 单独拎出来问用户，不自行编造 |
| 同一活动Excel多行板块不一致 | 问用户以哪行为准 |
| 用户没提供姓名 | 停下来等待，不猜测、不从文件名推断 |
| OCR找不到姓名 | 运行locate_name.py（自动升zoom 2.0→2.5→3.0→3.5）；仍找不到列入not_found问用户是否排除 |
| 疑似同名（有姓名无学号匹配） | 配置target_id后重新定位；确认学号是否正确 |
| 说明页无公章 | check_stamp.py提示后告知用户确认原文件；电子证明模板可接受，纸质应有章则提示检查扫描 |
| 说明和名字在同一页紧挨着 | 用single_page模式，只截一张 |
| 红框偏了 | 调整box_pad参数（默认12，可改成15-20） |
| 临时截图残留 | 检查输出目录的_tmp_*.png并手动删除（技能脚本已自动清理，异常中断时兜底） |
| 输出Word被Word/WPS占用 | 提示用户关闭文档后重试 |
| 用户只要截图不要分类 | 跳过第一到第四步，直接从第五步开始 |
